// Baseline applied to every Skein module: JVM/Kotlin compiler settings, test defaults, Detekt.
plugins {
    id("skein.java-conventions")
    id("skein.testing-conventions")
    id("skein.detekt-conventions")
}
