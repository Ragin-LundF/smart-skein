# IDEs
.idea/*
*.iws
*.iml
*.ipr
!.idea/inspectionProfiles
!.idea/inspectionProfiles/*
!.idea/detekt.xml
!.run/
.vscode/*

# Build targets
.gradle/*
buildSrc/.gradle
**/build/*
**/target/*
**/bin/*

# Other
.kotlin
logs/*
/.gradle/
/build/
.settings
.classpath
.project
.plan

# Release / signing — never commit keys (the release workflow materializes these from secrets)
ragin_*.pgp
*.gpg
*.skein
skein-demo
localSettings.gradle.kts

# skein-demo's RuleTrainerTest writes a timestamped CSV of internal training rows into the repo
# root. The module itself is ignored above; its output was not, which left real records one
# `git add -A` away from a public repository.
training_data_*.csv
